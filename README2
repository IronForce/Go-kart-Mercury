Una vez instalado GIT, debemos configurarlo:

* git config --global user.name "usuario"
* git config --global user.email "correo"

Generando tu Public Key:

* ssh-keygen

Leyendo tu clave para copiarla a Github:

* cat SSHKey.pub

Arrancando tu proyecto:

* git init

touch README # crea archivo

* git add README

* git commit -m "texto o explicativo de los cambios que estas subiendo a gitHUB"

push es para enviar archivos y pull para jalar archivos

* git push origin master / git pull origin master
